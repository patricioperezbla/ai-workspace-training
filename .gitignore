node_modules/
dist/
.tmp/
demos/.tmp/

# Exported PDFs — exclude root-level, but keep public/ version for GitHub Pages
*.pdf
!public/*.pdf

# Keep GIFs — they're the demo assets served by GitHub Pages
!public/demos/*.gif
